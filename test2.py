names = ['Jack', "frank", "Jeff", "Jasmine"]

def stuff(list):
    for name in list:
      if len(name)>5 and ('n' in name or 'N' in name):
        print(len(name)
    while (len(list) > 0):
      print(list.pop())
    print list

stuff(names)